# dev_ohne_jekyll

Dieses Verzeichnis enthaelt die alten Prototyp-Seiten ohne Jekyll-Architektur.

Zweck:
- Referenz fuer fruehere Layouts
- Vergleich waehrend der Migration

Aktive Entwicklung erfolgt ab jetzt ausschliesslich in `dev/` mit Jekyll (`md`, `layouts`, `includes`).
